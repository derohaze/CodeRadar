/**
 * Fixture: intentionally contains a credential-shaped literal so the secret
 * detector has something real to find. The value below is fake, is not a
 * working key for any service, and must never be replaced with a real one.
 */

export const providerConfig = {
  endpoint: "https://api.example.com/v1",
  apiKey: "sk-test-4f9a2b7c1d8e3a5b6c0d",
};
